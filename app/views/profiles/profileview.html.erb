
<div style="padding-left: 10px; padding-top: 60px ">
<h1>user profile</h1>
<%= image_tag @user.profile.avatar.url(:square) %>
<table>
  <thead>
    <tr>
      <th>name</th>
      <th>date of birth</th>
      <th>Father Name</th>
      <th>Address</th>
      <th>Phone No</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    
      <tr>
        <td><%= @user.profile.name %></td>
        <td><%= @user.profile.dob %></td>
        <td><%= @user.profile.father_name %></td>
        <td><%= @user.profile.address %></td>
        <td><%= @user.profile.phone_number %></td>
        </tr>
  </tbody>
</table>

<h3> your belt history </h3>
 <table>
  <thead>
    <tr>
      <th>belt</th>
      <th>pass/fail</th>
      <th>Location</td>
    </tr>
  </thead>
  <tbody>
  <% @belts.each do |post| %>    
    <tr>
 <td><%= post.type  %></td>
  <td><%= post.status %></td>
  <td><%= post.location %></td>
    </tr>
 <% end %>
</tbody>
</table>

<h2><%= link_to "home", root_path %></h2> /  <h2><%= link_to "Edit", {:controller => 'profiles' , :action => 'edit', :id => @user.profile.id} %></h2>

</div>